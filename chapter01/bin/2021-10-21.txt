2021-10-21 14:02 THU

chapter01 = java project
ch01 = package
warrior = class

행동을 메서드?

객체 : 상태와 행위를 가질수 있는 것 

자바는 기본적으로 중괄호안에 작성해야함

new 키워드 : JVM에서 heap영역(메모리 공간)을 불러서 Warrior w1을 저장함. 메모리에 저장한다는 뜻

w1에는 "주소값"이 담긴다.

------------
함수와 메서드

함수란 (function)

​● 하나의 기능을 수행하는 일련의 코드

● 구현된(정의된) 함수는 호출하여 사용하고 호출된 함수는 기능이 끝나면 제어가 반환됨

● 함수로 구현된 하나의 기능은 여러 곳에서 동일한 방식으로 호출되어 사용될 수 있음

------------
void = '텅 빈'
------------
함수 호출과 스택 메모리

● 스택 : 함수가 호출될 때 지역 변수들이 사용하는 메모리,
	메모리 구조

● 함수의 수행이 끝나면 자동으로 반환 되는 메모리 = 스택 
---------------
메서드 (method)

​● 객체의 기능을 구현하기 위해 클래스 내부에 구현되는 함수

● 멤버 함수 (member function)이라고도 함

● 메서드를 구현함으로써 객체의 기능이 구현 됨






